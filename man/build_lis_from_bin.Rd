% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BasicLisCheck.R, R/build_lis_from_bin.R
\name{build_lis_from_bin}
\alias{build_lis_from_bin}
\title{Build .lis files from DayCent .bin outputs (eq, base, exp), read, combine, save, cleanup}
\usage{
build_lis_from_bin(
  site,
  scen,
  dc_L100,
  lis_vars = c("som1c(2)", "som2c(2)", "som3c", "somsc", "agcprd", "cgrain"),
  out_dir = "outputs",
  keep_combined = TRUE
)

build_lis_from_bin(
  site,
  scen,
  dc_L100,
  lis_vars = c("som1c(2)", "som2c(2)", "som3c", "somsc", "agcprd", "cgrain"),
  out_dir = "outputs",
  keep_combined = TRUE
)
}
\arguments{
\item{site}{character. Site name (e.g., "wooster")}

\item{scen}{character. Scenario/run label used in schedule/run call (e.g., "cc_nt")}

\item{dc_L100}{character. Path to DDlist100 executable}

\item{out_dir}{character. Output folder (default "outputs")}

\item{keep_combined}{logical. Write combined tables to out_dir (default TRUE)}

\item{run}{character. Experiment/run label used in schedule/run call (e.g., "cc_nt")}

\item{eq_base_start_year}{numeric. Base start year threshold for labeling (default 1841)}

\item{exp_start_year}{numeric. Experiment start year threshold for labeling (default 1963)}

\item{eq_year_offset}{numeric. Adjustment used in your lab to shift eq years (default 1841-452)}

\item{lis_vars_path}{character. Path to outvars list file (e.g., "few_outvars.txt")}
}
\value{
list with: all (df), base_exp (df), exp (df), logs (list)

list with: all (df), base_exp (df), exp (df), logs (list)
}
\description{
Build .lis files from DayCent .bin outputs (eq, base, exp), read, combine, save, cleanup
}
