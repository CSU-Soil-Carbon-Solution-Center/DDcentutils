% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runDayCent_api.R
\name{runDayCent_api}
\alias{runDayCent_api}
\title{Run one DayCent site/scenario through the EMDC API}
\usage{
runDayCent_api(
  site,
  scen,
  run_eq = FALSE,
  config,
  project_path = ".",
  name = "daycent-r-api-run",
  wait = TRUE,
  keep_zip = FALSE,
  timeout_seconds = 3600,
  output_zip = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{site}{Character site name.}

\item{scen}{Character scenario name.}

\item{run_eq}{Logical. Request equilibrium/base processing before the scenario.}

\item{config}{API configuration from \code{\link[=daycent_runner_config]{daycent_runner_config()}}.}

\item{project_path}{Project root containing \verb{sites/} and optionally \verb{libs/}.}

\item{name}{Descriptive API run name.}

\item{wait}{Logical. Wait for completion and download results when \code{TRUE}.}

\item{keep_zip}{Logical. Retain the downloaded result ZIP.}

\item{timeout_seconds}{Maximum status-watch duration in seconds.}

\item{output_zip}{Optional result ZIP destination.}

\item{overwrite}{Logical. Allow existing result ZIPs and outputs to be replaced.}
}
\value{
With \code{wait = FALSE}, submission information containing \code{run_id}.
With \code{wait = TRUE}, a list containing submission, terminal status, and
extracted result paths.
}
\description{
Stages one exact \code{site/scenario} selection, submits it to the ModelRuns API,
and optionally watches and downloads the result. The API supports
equilibrium through \code{run_eq}; an independent base-only run is not exposed.
}
