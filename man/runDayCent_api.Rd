% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runDayCent_api.R
\name{runDayCent_api}
\alias{runDayCent_api}
\title{Run selected DayCent site/scenario pairs through the EMDC API}
\usage{
runDayCent_api(
  include = NULL,
  run_eq = FALSE,
  run_base = run_eq,
  config,
  project_path = ".",
  name = "daycent-r-api-run",
  wait = TRUE,
  keep_zip = FALSE,
  timeout_seconds = 3600,
  output_zip = NULL,
  overwrite = FALSE,
  dry_run = FALSE,
  dry_run_mode = "Full"
)
}
\arguments{
\item{include}{Character vector of exact \code{site/scenario} selections. \code{NULL},
empty, or \code{"*"} selects all pairs discovered by the API.}

\item{run_eq}{Logical. Request equilibrium and base processing before each
selected scenario.}

\item{run_base}{Logical. Must equal \code{run_eq} for API runs because the API
exposes one \code{runEquilibrium} field rather than an independent base flag.}

\item{config}{API configuration from \code{\link[=daycent_runner_config]{daycent_runner_config()}}.}

\item{project_path}{Project root containing \verb{sites/} and optionally \verb{libs/}.}

\item{name}{Descriptive API run name.}

\item{wait}{Logical. Wait for completion and download results when \code{TRUE}.}

\item{keep_zip}{Logical. Retain the downloaded result ZIP.}

\item{timeout_seconds}{Maximum status-watch duration in seconds.}

\item{output_zip}{Optional result ZIP destination.}

\item{overwrite}{Logical. Allow existing result ZIPs and outputs to be replaced.}

\item{dry_run}{Logical. Submit preflight validation without creating a run.}

\item{dry_run_mode}{Optional API mode: \code{Full}, \code{CreditCostOnly}, or \code{InputQcOnly}.}
}
\value{
With \code{dry_run = TRUE}, preflight evidence with no \code{run_id}.
Otherwise, with \code{wait = FALSE}, submission information containing
\code{run_id}; with \code{wait = TRUE}, a list containing submission, terminal
status, and extracted result paths.
}
\description{
Stages one or more exact \code{site/scenario} selections, submits one ModelRuns
request, and optionally watches and downloads the result. The API represents
equilibrium and base as one paired choice; an independent base-only run is
not exposed.
}
