% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_library100.R
\name{read_library100}
\alias{read_library100}
\title{Read a DayCent library (.100) file's block IDs}
\usage{
read_library100(path)
}
\arguments{
\item{path}{Path to a \code{.100} library file.}
}
\value{
A list with elements \code{path}, \code{block_ids} (character vector, one
entry per block-ID line, in file order -- duplicates included), and
\code{duplicate_ids} (character vector of IDs that appeared more than once,
each listed once).
}
\description{
Reads a DayCent \code{.100} library file (\code{crop.100}, \code{fert.100}, \code{harv.100},
etc.) well enough to resolve the block IDs schedule events reference --
it does not parse parameter values. A line is a block-ID line unless it
is blank, a \verb{#} comment, or starts with a number; the ID is the leading
\verb{[A-Za-z][A-Za-z0-9_]*} run. IDs are collected in file order, including
repeats; any ID seen more than once is additionally reported in
\code{duplicate_ids}.
}
