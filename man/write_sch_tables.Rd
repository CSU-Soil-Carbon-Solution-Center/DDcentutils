% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sch_tables.R
\name{write_sch_tables}
\alias{write_sch_tables}
\title{Write an \code{sch} object's tables to CSV}
\usage{
write_sch_tables(sch, dir)
}
\arguments{
\item{sch}{An \code{sch} object (or an object coercible via \code{\link[=as_sch]{as_sch()}}).}

\item{dir}{Directory to write \code{site.csv} / \code{blocks.csv} / \code{events.csv}
into. Created if it does not already exist.}
}
\value{
\code{dir}, invisibly.
}
\description{
Writes \code{site_table}, \code{block_table}, and \code{event_table} to \code{site.csv},
\code{blocks.csv}, and \code{events.csv} in \code{dir}. Thin by design: the engine
(\code{\link[=build_sch]{build_sch()}}, \code{\link[=write_sch]{write_sch()}}) takes data.frames directly, so the file
format here is swappable without touching schedule-building logic.
}
