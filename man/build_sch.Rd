% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sch.R
\name{build_sch}
\alias{build_sch}
\title{Build a schedule from tidy tables}
\usage{
build_sch(site_table, event_table, block_table = NULL)
}
\arguments{
\item{site_table}{A one-row data.frame. Must include \code{start_year},
\code{end_year}, \code{site_file}; other required columns are filled from
documented defaults when absent (see Details).}

\item{event_table}{A data.frame with the columns from
\code{sch_columns("event")}.}

\item{block_table}{Optional data.frame with the columns from
\code{sch_columns("block")}. When \code{NULL} (the default), a single block is
derived from \code{site_table}.}
}
\value{
An object of class \code{"sch"}.
}
\description{
The tidy-table entry point for constructing a DayCent schedule:
\code{site_table} (one row) plus \code{event_table} (one row per event), with an
optional \code{block_table}. When \code{block_table} is omitted, a single default
block spanning the full \code{start_year}-\code{end_year} range is derived. Missing
\code{site_table} columns that have a documented default (see Details) are
filled in; \code{start_year}, \code{end_year}, and \code{site_file} have no default and
must be supplied.
}
\details{
Default values used to fill missing \code{site_table} columns:
\code{labeling_type = 0}, \code{labeling_year = -1}, \code{microcosm = -1.00},
\code{co2_systems = -1}, \code{ph_effect = -1}, \code{soil_warming = -1},
\code{n_input_scalar_option = 0}, \code{omad_scalar_option = 0},
\code{climate_scalar_option = 0}, \code{initial_system = 1}, \code{initial_tree = ""}.
\code{extra_header_lines} defaults to an empty list-column when absent.
\code{initial_crop} has no default and, like any other truly missing required
column, surfaces as an error from the underlying \code{new_sch()} call.

\code{site_table} may optionally include a \code{weather_file} column; when
\code{block_table} is omitted, its (single) value determines the derived
block's \code{weather_choice} (\code{"F"} when non-\code{NA}, otherwise \code{"C"}) and
\code{weather_file}. This column is a build-time convenience only and is not
part of the final \code{sch} object's \code{site_table}.
}
