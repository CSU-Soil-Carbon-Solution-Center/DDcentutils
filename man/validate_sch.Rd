% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_sch.R
\name{validate_sch}
\alias{validate_sch}
\title{Validate a DayCent schedule}
\usage{
validate_sch(sch, library_dir = NULL, site_dir = NULL, policy = "standard")
}
\arguments{
\item{sch}{An \code{sch} object (or an object coercible via \code{\link[=as_sch]{as_sch()}}).}

\item{library_dir}{Optional path to a directory of \code{.100} library files
(as read by \code{\link[=read_library_dir]{read_library_dir()}}). Library checks are skipped when
\code{NULL}.}

\item{site_dir}{Optional path to the site directory weather references
are expected to resolve within. Weather-reference checks are skipped
when \code{NULL}.}

\item{policy}{One of \code{"standard"}, \code{"strict_mrv"}, \code{"exploratory"} --
accepted for forward compatibility with the platform's policy concept.
None of the currently vendored rules define a policy override, so this
has no effect yet.}
}
\value{
A data.frame with columns \code{code}, \code{severity}, \code{file}, \code{line},
\code{message}, \code{context} (a list-column), one row per finding.
}
\description{
Structural and semantic checks over an \code{sch} object, ported from the
EMDC preflight platform's \code{schfile_checks.py} and \code{schedule_qc.py}.
Accumulates every finding rather than stopping at the first -- that
matches the platform design and is the point of the exercise.

Checks performed:
\itemize{
\item structural: an explicit parse failure (\code{SCH_PARSE_ERROR}, which
returns early -- nothing else is reliable), an empty/unparseable
header (\code{SCH_HEADER_INVALID}), \code{end_year < start_year}
(\code{SCH_RUN_LENGTH_DERIVATION_FAIL})
\item events: unknown event vocabulary (\code{SCH_UNKNOWN_EVENT}), invalid
day-of-year (\code{SCH_EVENT_DOY_INVALID}; \code{366} is legal, \verb{367+} and \verb{<1}
are not), unpaired crop/tree start-end events
(\code{SCH_EVENT_PAIRING_INVALID})
\item libraries (only when \code{library_dir} is supplied): a resolved
event token absent from its library (\code{SCH_EVENT_REF_NOT_IN_LIBRARY}),
a library file an event needs that is not present
(\code{LIB100_REQUIRED_FILE_MISSING}). Immediate-input arguments (e.g.
\verb{FERT (3.4N)}) are resolved per \code{lookup_ids_for_event()} before
lookup; matching is exact, never case-folded.
\item weather references (only when \code{site_dir} is supplied): a
reference that is absolute, contains \code{..}, or resolves outside
\code{site_dir} (\code{SCH_WEATHER_REF_OUTSIDE_SITE_DIR}), or does not exist
(\code{SCH_WEATHER_REF_MISSING})
}

An \code{sch} with no \code{parsed_ok} metadata at all (e.g. one built with
\code{\link[=build_sch]{build_sch()}} rather than read from a file) is treated as not-yet-parsed
rather than failed -- the parse-failure check only fires when
\code{parsed_ok} is explicitly \code{FALSE}.

When \code{library_dir} is \code{NULL}, all library checks are skipped and one
\code{INFO} finding notes it, so the validator stays useful without a
licensed DayCent library set.
}
