% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_sch.R
\name{write_sch}
\alias{write_sch}
\title{Render an \code{sch} object to DayCent schedule text}
\usage{
write_sch(sch, path = NULL, value_width = 14)
}
\arguments{
\item{sch}{An \code{sch} object (or an object coercible via \code{\link[=as_sch]{as_sch()}}).}

\item{path}{Optional. If given, the rendered text is written to this
path (one schedule element per line, no trailing file-level
terminator).}

\item{value_width}{Integer. Column width the header/block-header value
is left-padded to before the label. Cosmetic only — DayCent reads the
first whitespace-delimited token. Defaults to \code{14}.}
}
\value{
The rendered character vector (one schedule line per element),
invisibly.
}
\description{
Renders an \code{\link[=new_sch]{new_sch()}} object back into DayCent \code{.sch} schedule text: the
15-field header (with any option-payload lines and extra header lines),
the \verb{Year Month Option} marker, and one block per row of \code{block_table}
with its events and \verb{-999 -999 X} terminator. This is the inverse of
\code{\link[=read_sch]{read_sch()}}.
}
