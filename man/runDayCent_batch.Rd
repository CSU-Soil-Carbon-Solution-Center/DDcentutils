% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runDayCent_batch.R
\name{runDayCent_batch}
\alias{runDayCent_batch}
\title{Run selected DayCent site/scenario pairs through a local or API backend}
\usage{
runDayCent_batch(
  project_path = ".",
  include = NULL,
  sites = NULL,
  scenarios = NULL,
  backend = NULL,
  config = NULL,
  run_eq = FALSE,
  run_base = run_eq,
  name = "daycent-r-api-batch",
  wait = TRUE,
  keep_zip = FALSE,
  timeout_seconds = 3600,
  output_zip = NULL,
  overwrite = FALSE,
  workers = 1L,
  error_policy = c("stop", "collect"),
  executor = NULL,
  parent_run_id = NULL,
  ...
)
}
\arguments{
\item{project_path}{Project root containing \verb{sites/}.}

\item{include}{Exact \code{site/scenario} selectors. \code{NULL}, empty, or \code{"*"}
selects all discovered pairs.}

\item{sites}{Optional site names for an explicit Cartesian selection.}

\item{scenarios}{Optional scenario names for an explicit Cartesian selection.}

\item{backend}{Either \code{"exe"} or \code{"api"}. When omitted, uses
\code{config$backend} when available and otherwise defaults to \code{"exe"}.}

\item{config}{Optional runner configuration.}

\item{run_eq}{Logical. Run equilibrium and base phases for every task.}

\item{run_base}{Logical. Local-only base phase flag; API runs must match
\code{run_eq}.}

\item{name}{API run name.}

\item{wait}{Logical. Wait for API completion; local runs always execute.}

\item{keep_zip}{Logical. Retain an API result ZIP.}

\item{timeout_seconds}{API watch timeout.}

\item{output_zip}{Optional API result ZIP path.}

\item{overwrite}{Logical. Allow API outputs and ZIPs to be replaced.}

\item{workers}{Local worker count. Defaults to sequential execution.}

\item{error_policy}{Either \code{"stop"} or \code{"collect"} for local failures.}

\item{executor}{Optional injected parallel executor with signature
\verb{function(tasks, worker, workers)}.}

\item{parent_run_id}{Optional API parent run ID to resume.}

\item{...}{Arguments forwarded to the atomic local runner.}
}
\value{
A list with \code{task_table}, \code{backend}, and parent API metadata when
applicable. Task rows preserve selector order and contain site, scenario,
status, parent/child IDs, output path, and error.
}
\description{
Run selected DayCent site/scenario pairs through a local or API backend
}
