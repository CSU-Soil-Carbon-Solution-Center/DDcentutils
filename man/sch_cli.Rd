% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sch_cli.R
\name{sch_cli}
\alias{sch_cli}
\title{Command-line interface for building, validating, and inspecting schedules}
\usage{
sch_cli(argv = commandArgs(trailingOnly = TRUE))
}
\arguments{
\item{argv}{Character vector of command-line arguments, usually
\code{commandArgs(trailingOnly = TRUE)}.}
}
\value{
Integer exit code: \code{0} clean (or only \code{WARN}/\code{INFO} findings),
\code{1} if \code{validate} found any \code{ERROR}, \code{2} on a usage error. \code{--help}
and no arguments print usage and return \code{0}.
}
\description{
Shell access to the schedule engine: \code{build} (tables -> \code{.sch}),
\code{validate} (\code{.sch} -> findings), and \code{inspect} (\code{.sch} -> tables). See
\code{inst/scripts/ddcent-sch.R} for the shell entry point, or call this
directly as \verb{Rscript -e 'DDcentutils::sch_cli()' --args <subcommand> ...}.

\code{inspect} closes the loop with \code{build}: parse an existing schedule to
tables, edit them, rebuild.

Execution (running DayCent itself) is out of scope here -- that stays
with \code{\link[=DayCentRunSite_single_run]{DayCentRunSite_single_run()}} / \code{\link[=runDayCent]{runDayCent()}}.
}
